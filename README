Use scripts/build.sh to build. _install is root filesystem.  
After boot kernel with QEMU. /test contain test program and test scripts.  
You can use scripts in /test/scripts to reproduce the test result.  
